---
# Home Assistant version to install (defaults to 'stable' if not specified)
home_assistant_version: "stable"

# Directory where Home Assistant configuration will be stored
home_assistant_config_dir: "/opt/homeassistant"

# System timezone
timezone: "UTC"

# Docker Compose version to use
docker_compose_version: "2.17.2"